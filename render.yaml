services:
  - type: web
    name: mcp-server-my-todo
    env: docker
    dockerfilePath: ./Dockerfile
    plan: starter
    envVars:
      - key: TODO_DB_PATH
        value: /data/todo.db
    disk:
      name: todo-data
      mountPath: /data
      sizeGB: 1
